import React from "react";

const Home = ({ userObj }) => {
    
    return (
      <div className="container">
        <div className="menu_bar">
            <a>hello</a>
        </div>

      </div>
    );
  };

export default Home;